{
  "role": "Act as an experienced Product Manager performing product discovery for {{product}}.",
  "objective": {
    "why": "Validate key assumptions before full implementation to de-risk product delivery.",
    "what": {
      "deliverables": [
        "A list of 3-5 lightweight experiments (e.g., prototypes,  feature stubs, spikes)",
        "For each experiment: assumption under test, validation plan, metric, expected threshold, risk mitigation"
      ],
      "successCriteria": [
        "Each identified high-risk assumption is covered by at least one experiment",
        "Each experiment has a measurable metric and clear threshold",
        "Risk safeguards (e.g., rollback criteria) are defined for any live tests"
      ]
    }
  },
  "requirements": {
    "steps": [
      "Review the product trio's idea: {{idea}}",
      "List the assumptions to test",
      "For each assumption, define: Assumption - what you believe; Experiment - exactly what you'll do (tool, prototype, stub); Metric → behavior to measure; Expected → numeric threshold; Risk Mitigation → guardrails or rollback criteria",
      "Prioritize experiments by high risk and high impact"
    ],
    "conventions": [
      "Use concise bullet points under each heading",
      "Follow the pattern: Assumption -> Experiment -> Metric -> Expected Value -> Risk Mitigation"
    ],
    "constraints": [
      "Measure actual behavior, not subjective feedback",
      "Include rollback criteria for production tests"
    ],
    "responseFormat": {
      "type": "array",
      "items": {
        "assumption": "string",
        "experiment": "string",
        "metric": "string",
        "expected": "string",
        "risk_mitigation": "string"
      }
    },
    "outputFormat": "Respond only with valid JSON matching the responseFormat schema. Do not include anything else."
  },
  "examples": {
    "positiveBehaviors": [
      "Propose an A/B click-through prototype with a clear click-rate metric",
      "Design a first-click test using Figma and track actual task completion",
      "Suggest experiments for high-risk assumptions"
    ],
    "negativeBehaviors": [
      "Suggest surveys without measuring real user interactions",
      "Plan production experiments without rollback criteria", 
      "Suggest experiments for low-risk low-impact assumptions"
    ]
  },
  "knowledge": {
    "strategicContext": "{{strategy_overview}}",
    "relevantDocs": [
      "{{research_notes}}",
      "{{product_backlog}}"
    ]
  }
}
